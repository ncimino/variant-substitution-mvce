plugins {
    id 'maven-publish'
    id 'cpp-library'
}

version = "3.2.1"

group 'com.company'

publishing {
    repositories {
        maven {
            name "local"
            url "../test-repo"
        }
    }
}

